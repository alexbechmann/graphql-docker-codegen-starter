{
  "name": "danfoss-forms-service",
  "version": "1.0.0",
  "description": "Working dir: ./api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run dev",
    "dev": "npm run docker",
    "docker": "docker-compose up --build",
    "postinstall": "npm-recursive-install --skip-root"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5",
    "recursive-install": "^1.4.0"
  }
}
